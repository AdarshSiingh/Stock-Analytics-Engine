[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "cd server && npm install",
  "pip install pandas numpy ta yfinance sqlalchemy pymysql python-dotenv"
]

[start]
cmd = "cd server && node index.js"